# Python dependencies for the music backend API
# These should be installed in the same environment as the music-embeddings and vector-search modules

# Core dependencies (likely already installed with the other modules)
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Audio processing (from music-embeddings module)
librosa>=0.9.0
soundfile>=0.10.0

# Vector search (from vector-search module)
faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4  # Uncomment for GPU support

# Additional utilities
tqdm>=4.62.0
joblib>=1.1.0

# Note: The actual music-embeddings and vector-search modules should be
# available in the parent directories as specified in the configuration
